---
import "../styles/global.css";
import AdminSidebar from '../components/dashboard/DashboardSidebar.astro';
import { ClientRouter } from "astro:transitions";

interface Props {
	title: string;
}

const { title } = Astro.props;
---

<!doctype html>
<html lang="es">
	<head>
		<meta charset="UTF-8" />
		<meta name="description" content="Panel de Administración F1 Live" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="generator" content={Astro.generator} />
		<title>{title} | Admin - F1 Live</title>
	</head>
	<body class="bg-bg text-text font-sans min-h-screen">
		<div class="flex min-h-screen">
			<!-- Sidebar Area (Placeholder para el siguiente paso) -->
			<aside class="w-[260px] bg-surface border-r border-white/5 flex flex-col shrink-0">
				<div class="p-6 border-b border-white/5 font-bold text-xl tracking-wide">
					<span>F1 Admin</span>
				</div>
				<nav class="flex-1">
					<AdminSidebar />
				</nav>
			</aside>

			<!-- Main Content Area -->
			<main class="flex-1 p-8 overflow-y-auto">
				<slot />
			</main>
		</div>
	</body>
</html>