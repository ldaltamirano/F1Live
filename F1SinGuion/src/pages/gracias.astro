---
import Layout from "../layouts/Layout.astro";
import Button from "../components/ui/Button.astro";
---

<Layout title="¡Suscripción Confirmada! - F1 Sin Guion">
  <div class="bg-bg min-h-screen flex items-center justify-center px-4 relative overflow-hidden">
    
    {/* Efectos de fondo */}
    <div class="absolute inset-0 opacity-[0.03] pointer-events-none" style="background-image: radial-gradient(#ffffff 1px, transparent 1px); background-size: 4px 4px;"></div>
    <div class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[600px] h-[600px] bg-green-500/10 blur-[120px] rounded-full pointer-events-none"></div>

    <div class="max-w-2xl w-full relative z-10 text-center">
      
      <div class="mb-8 animate-bounce">
        <span class="text-8xl">🏁</span>
      </div>

      <h1 class="text-4xl md:text-6xl font-black text-white mb-6 uppercase italic tracking-tighter">
        ¡Ya estás en la <span class="text-transparent bg-clip-text bg-gradient-to-r from-green-400 to-cyan-500">pole position!</span>
      </h1>
      
      <div class="bg-surface backdrop-blur-sm border border-white/10 rounded-2xl p-8 mb-8 shadow-2xl shadow-black/50">
        <p class="text-xl text-gray-300 mb-4">
          Tu lugar en la parrilla está reservado.
        </p>
        <p class="text-gray-400">
          Revisa tu bandeja de entrada (y la de spam por si acaso) para confirmar tu suscripción y no perderte la largada.
        </p>
      </div>

      <div class="flex justify-center gap-4">
        <Button href="/" variant="primary">
          Volver al Paddock
        </Button>
        <Button href="/noticias" variant="ghost">
          Leer Noticias
        </Button>
      </div>

    </div>
  </div>
</Layout>