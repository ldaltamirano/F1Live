---
const currentYear = new Date().getFullYear();
---

<footer class="footer">
  <div class="container">
    <p>&copy; {currentYear} F1 Sin Guion. Todos los derechos reservados.</p>
    <div class="links">
      <!-- Aquí irán enlaces reales en el futuro -->
      <a href="#">Aviso Legal</a>
      <a href="#">Privacidad</a>
      <a href="#">Contacto</a>
    </div>
  </div>
</footer>

<style>
  .footer {
    background-color: var(--color-surface);
    border-top: 1px solid var(--border-color);
    padding: 2rem 0;
    margin-top: auto; /* Empuja el footer al fondo si hay poco contenido */
    color: var(--color-text-muted);
    font-size: 0.875rem;
  }

  .container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 1.5rem;
    display: flex;
    flex-direction: column;
    gap: 1rem;
    align-items: center;
  }

  @media (min-width: 768px) {
    .container {
      flex-direction: row;
      justify-content: space-between;
    }
  }

  .links {
    display: flex;
    gap: 1.5rem;
  }

  a {
    color: var(--color-text-muted);
    text-decoration: none;
    transition: color 0.2s;
  }

  a:hover {
    color: var(--color-primary);
  }
</style>