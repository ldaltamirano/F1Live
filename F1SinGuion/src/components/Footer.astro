---
import Newsletter from "./Newsletter.astro";
import SocialLinks from "./SocialLinks.astro";
const currentYear = new Date().getFullYear();

const navLinks = [
  { name: "Inicio", href: "/" },
  { name: "Live", href: "/live" },
  { name: "Circuitos", href: "/circuitos" },
  { name: "Escuderias", href: "/escuderias" },
  { name: "Pilotos", href: "/pilotos" },
  { name: "Resultados", href: "/resultados" },
];

const legalLinks = [
  { name: "Aviso Legal", href: "/aviso-legal" },
  { name: "Politica de privacidad", href: "/privacidad" },
  { name: "Términos y Condiciones", href: "/terminos" },
  { name: "Cookies", href: "/cookies" },
  { name: "Contacto", href: "/contacto" },
];
---

<footer class="bg-surface border-t border-white/10 pt-8 pb-8">
  <div class="container mx-auto px-4 sm:px-6 lg:px-8">
    
    {/* Newsletter Section */}
    <div class="mb-12 border-b border-white/10 pb-3">
      <Newsletter />
    </div>

    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-5 gap-12 mb-12">
      <!-- Brand Section -->
      <div class="space-y-4 lg:col-span-2">
        <a href="/" class="block">
          <img src="/logo.png" alt="F1 Live" class="h-12 w-auto opacity-90 hover:opacity-100 transition-opacity" />
        </a>
        <p class="text-text-muted text-sm leading-relaxed">
          Tu destino definitivo para seguir la Fórmula 1 en tiempo real. Noticias, resultados, estadísticas y mucho más, sin guion.
        </p>
      </div>

      <!-- Navigation -->
      <div>
        <h3 class="text-white font-bold uppercase tracking-widest mb-4">Explorar</h3>
        <ul class="space-y-2">
          {navLinks.map(link => (
            <li>
              <a href={link.href} class="text-text-muted hover:text-accent transition-colors text-sm">
                {link.name}
              </a>
            </li>
          ))}
        </ul>
      </div>

      <!-- Legal -->
      <div>
        <h3 class="text-white font-bold uppercase tracking-widest mb-4">Legal</h3>
        <ul class="space-y-2">
          {legalLinks.map(link => (
            <li>
              <a href={link.href} class="text-text-muted hover:text-accent transition-colors text-sm">
                {link.name}
              </a>
            </li>
          ))}
        </ul>
      </div>

      <!-- Social -->
      <div class="lg:text-right">
        <h3 class="text-white font-bold uppercase tracking-widest mb-4">Síguenos</h3>
        <SocialLinks class="lg:justify-end" />
      </div>
    </div>

    <div class="border-t border-white/10 pt-8 flex flex-col md:flex-row justify-between items-center gap-4">
      <p class="text-text-muted text-sm">&copy; {currentYear} F1 Sin Guion. Todos los derechos reservados.</p>
      <p class="text-text-muted text-xs">Hecho con 🏁 para fans.</p>
    </div>
  </div>
</footer>